# Temporary smoke test: isolates whether macOS runners provision for this repo.
# ubuntu-latest already succeeded; this tests macos-14 alone.
# Delete once the release pipeline is confirmed working.
name: ci-smoke

on:
  push:
  workflow_dispatch:

jobs:
  macos14:
    runs-on: macos-14
    steps:
      - run: echo "macos-14 provisioned and ran"
